export default function Fail({children}) {
    return(
        <div style={{marginTop: '10px',padding: '0.4rem', color: '#fff',border:'2px solid #000', borderRadius:'5px', 
        backgroundColor: 'rgb(220, 20, 60)', textAlign: 'center', fontWeight: '700'}}>{children}</div>
    )
}